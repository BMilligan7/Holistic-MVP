import './index.css';

function App() {
  return (
    <>
      {/* 
        App component is effectively no longer used for routing or layout.
        This is now handled by src/main.tsx (via RouterProvider) 
        and src/router.tsx (which defines routes and uses Layout.tsx).
        This file can be further cleaned up or removed if no other part of the system imports it.
      */}
    </>
  );
}

export default App; 